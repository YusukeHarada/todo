export type Category = {
    id: string;
    userId: string;
    name: string;
    color: string;
    createdAt: string;
};

export type CategoryFormValues = {
    name: string;
    color: string;
};
